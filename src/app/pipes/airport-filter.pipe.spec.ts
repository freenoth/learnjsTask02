import { AirportFilterPipe } from './airport-filter.pipe';

describe('AirportFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new AirportFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
