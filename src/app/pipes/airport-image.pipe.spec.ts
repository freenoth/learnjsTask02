import { AirportImagePipe } from './airport-image.pipe';

describe('AirportImagePipe', () => {
  it('create an instance', () => {
    const pipe = new AirportImagePipe();
    expect(pipe).toBeTruthy();
  });
});
